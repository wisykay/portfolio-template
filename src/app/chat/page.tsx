import { ChatWindow } from "./components/ChatWindow";

export const metadata = {
  title: "Chat — Portfolio",
  description: "Explore this portfolio through an AI-powered conversation.",
};

export default function ChatPage() {
  return <ChatWindow />;
}
